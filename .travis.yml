sudo: required
services:
- docker

deploy:
- provider: script
  script: bash docker_push latest
  on:
    branch: master
    repo: openpitrix/runtime-provider-qingcloud
- provider: script
  script: bash docker_push $TRAVIS_TAG
  on:
    tags: true
    repo: openpitrix/runtime-provider-qingcloud